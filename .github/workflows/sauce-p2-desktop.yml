name: sauce-p2-desktop

on: [push]

jobs:
  tests:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - uses: actions/setup-node@v1
      with:
        node-version: 12

    - name: Install CLI tools
      run: npm install -g bower

    - name: Install dependencies
      run: npm -q i

    - name: Install Bower
      run: bower -q i

#    - name: Run unit tests [Desktop / Batch 1]
#      env:
#        SAUCE_USERNAME: ${{ secrets.SAUCE_USERNAME }}
#        SAUCE_ACCESS_KEY: ${{ secrets.SAUCE_ACCESS_KEY }}
#      run: npm test -- --env saucelabs:desktop --suite 'test/index.batch1.html'
#
#    - name: Run unit tests [Desktop / Batch 2]
#      env:
#        SAUCE_USERNAME: ${{ secrets.SAUCE_USERNAME }}
#        SAUCE_ACCESS_KEY: ${{ secrets.SAUCE_ACCESS_KEY }}
#      run: npm test -- --env saucelabs:desktop --suite 'test/index.batch2.html'
#
#    - name: Run unit tests [Desktop / Batch 3]
#      env:
#        SAUCE_USERNAME: ${{ secrets.SAUCE_USERNAME }}
#        SAUCE_ACCESS_KEY: ${{ secrets.SAUCE_ACCESS_KEY }}
#      run: npm test -- --env saucelabs:desktop --suite 'test/index.batch3.html'
#
#    - name: Run unit tests [Desktop / Batch 4]
#      env:
#        SAUCE_USERNAME: ${{ secrets.SAUCE_USERNAME }}
#        SAUCE_ACCESS_KEY: ${{ secrets.SAUCE_ACCESS_KEY }}
#      run: npm test -- --env saucelabs:desktop --suite 'test/index.batch4.html'

    - name: Run unit tests [Mobile / Batch 1]
      env:
        SAUCE_USERNAME: ${{ secrets.SAUCE_USERNAME }}
        SAUCE_ACCESS_KEY: ${{ secrets.SAUCE_ACCESS_KEY }}
      run: npm test -- --env saucelabs:mobile --suite 'test/index.batch1.html'

    - name: Run unit tests [Mobile / Batch 2]
      env:
        SAUCE_USERNAME: ${{ secrets.SAUCE_USERNAME }}
        SAUCE_ACCESS_KEY: ${{ secrets.SAUCE_ACCESS_KEY }}
      run: npm test -- --env saucelabs:mobile --suite 'test/index.batch2.html'

    - name: Run unit tests [Mobile / Batch 3]
      env:
        SAUCE_USERNAME: ${{ secrets.SAUCE_USERNAME }}
        SAUCE_ACCESS_KEY: ${{ secrets.SAUCE_ACCESS_KEY }}
      run: npm test -- --env saucelabs:mobile --suite 'test/index.batch3.html'

    - name: Run unit tests [Mobile / Batch 4]
      env:
        SAUCE_USERNAME: ${{ secrets.SAUCE_USERNAME }}
        SAUCE_ACCESS_KEY: ${{ secrets.SAUCE_ACCESS_KEY }}
      run: npm test -- --env saucelabs:mobile --suite 'test/index.batch4.html'
