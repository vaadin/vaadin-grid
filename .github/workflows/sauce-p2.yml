name: sauce-p2

on: [push]

jobs:
  tests:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - uses: actions/setup-node@v1
      with:
        node-version: 12

    - name: Install CLI tools
      run: npm install -g bower

    - name: Install dependencies
      run: npm -q i

    - name: Install Bower
      run: bower -q i

    - name: Run unit tests [Desktop / Batch 1]
      env:
        SAUCE_USERNAME: ${{ secrets.SAUCE_USERNAME }}
        SAUCE_ACCESS_KEY: ${{ secrets.SAUCE_ACCESS_KEY }}
      run: npm test -- --env saucelabs:desktop --suites batch1

    - name: Run unit tests [Desktop / Batch 2]
      env:
        SAUCE_USERNAME: ${{ secrets.SAUCE_USERNAME }}
        SAUCE_ACCESS_KEY: ${{ secrets.SAUCE_ACCESS_KEY }}
      run: npm test -- --env saucelabs:desktop --suites batch2

    - name: Run unit tests [Desktop / Batch 3]
      env:
        SAUCE_USERNAME: ${{ secrets.SAUCE_USERNAME }}
        SAUCE_ACCESS_KEY: ${{ secrets.SAUCE_ACCESS_KEY }}
      run: npm test -- --env saucelabs:desktop --suites batch3

    - name: Run unit tests [Desktop / Batch 4]
      env:
        SAUCE_USERNAME: ${{ secrets.SAUCE_USERNAME }}
        SAUCE_ACCESS_KEY: ${{ secrets.SAUCE_ACCESS_KEY }}
      run: npm test -- --env saucelabs:desktop --suites batch4

    - name: Run unit tests [Desktop / Batch 5]
      env:
        SAUCE_USERNAME: ${{ secrets.SAUCE_USERNAME }}
        SAUCE_ACCESS_KEY: ${{ secrets.SAUCE_ACCESS_KEY }}
      run: npm test -- --env saucelabs:desktop --suites batch5

    - name: Run unit tests [Desktop / Polymer2 Only]
      env:
        SAUCE_USERNAME: ${{ secrets.SAUCE_USERNAME }}
        SAUCE_ACCESS_KEY: ${{ secrets.SAUCE_ACCESS_KEY }}
      run: npm test -- --env saucelabs:desktop --suites polymer2Only

#    - name: Run unit tests [Mobile / Batch 1]
#      env:
#        SAUCE_USERNAME: ${{ secrets.SAUCE_USERNAME }}
#        SAUCE_ACCESS_KEY: ${{ secrets.SAUCE_ACCESS_KEY }}
#      run: npm test -- --env saucelabs:mobile --suites batch1
#
#    - name: Run unit tests [Mobile / Batch 2]
#      env:
#        SAUCE_USERNAME: ${{ secrets.SAUCE_USERNAME }}
#        SAUCE_ACCESS_KEY: ${{ secrets.SAUCE_ACCESS_KEY }}
#      run: npm test -- --env saucelabs:mobile --suites batch2
#
#    - name: Run unit tests [Mobile / Batch 3]
#      env:
#        SAUCE_USERNAME: ${{ secrets.SAUCE_USERNAME }}
#        SAUCE_ACCESS_KEY: ${{ secrets.SAUCE_ACCESS_KEY }}
#      run: npm test -- --env saucelabs:mobile --suites batch3
#
#    - name: Run unit tests [Mobile / Batch 4]
#      env:
#        SAUCE_USERNAME: ${{ secrets.SAUCE_USERNAME }}
#        SAUCE_ACCESS_KEY: ${{ secrets.SAUCE_ACCESS_KEY }}
#      run: npm test -- --env saucelabs:mobile --suites batch4
#
#    - name: Run unit tests [Mobile / Batch 5]
#      env:
#        SAUCE_USERNAME: ${{ secrets.SAUCE_USERNAME }}
#        SAUCE_ACCESS_KEY: ${{ secrets.SAUCE_ACCESS_KEY }}
#      run: npm test -- --env saucelabs:mobile --suites batch5
