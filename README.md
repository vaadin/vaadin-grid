Here you have a set of project used for Vaadin Components

- vaadin-components-gwt
   Exported vaadin widgets from html and js

- Assuming you have installed maven, compile and package it just running:
$ mvn clean package
